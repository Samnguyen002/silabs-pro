---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "peripheral_devices"
steps:
- task: "create_gbl"
  output: "{{build_dir}}/{{project_name}}.gbl"
  app: "{{build_dir}}/{{project_name}}.out"
